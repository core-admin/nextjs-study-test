`revalidatePath` 清除的是 Next.js 的 **Router Cache** (路由缓存)，具体包括：

1. **客户端路由缓存**：
   - 浏览器内存中的路由段缓存
   - 预取的数据
   - React Server Component payload

2. **服务器端缓存**：
   - React Server Component payload 缓存
   - 数据请求缓存（fetch 请求）

在你的代码中：
```javascript:src/app/api/upload/route.js
revalidatePath('/', 'layout');
```
这行代码会：
- 清除根路径 `/` 相关的所有缓存
- `layout` 参数表示会同时清除该路径下所有子页面的缓存

使用场景：
- 当数据发生变化（如上传新文件）
- 需要强制页面刷新显示最新内容时

注意：这不会清除浏览器的 HTTP 缓存或 CDN 缓存。
